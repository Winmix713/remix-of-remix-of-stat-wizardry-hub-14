# Roadmap

- [x] Enable Lovable Cloud (old backend is gone)
- [x] Create `matches` table matching existing code + CSV structure
- [x] Import the 4 uploaded CSVs (960 matches)
- [x] Verify the app loads data
